# คัดลอกไปเป็น .env.local (ไม่ commit .env.local)

# Database (Prisma อ่านจาก .env หรือ .env.local)
# Supabase: ใช้ Pooler URL = DATABASE_URL, Session/Direct URL = DIRECT_URL
DATABASE_URL="postgresql://...?sslmode=require&pgbouncer=true"
DIRECT_URL="postgresql://...:5432/postgres?sslmode=require"

# Admin (ถ้าจะย้ายจากค่าในโค้ดไปใช้ env)
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=
