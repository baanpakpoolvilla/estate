# ตัวอย่างตัวแปรสภาพแวดล้อม (คัดลอกไปใช้ใน .env.local หรือตั้งใน Vercel)
# ไม่ต้อง commit ไฟล์ .env.local

# Database (ใช้เมื่อเชื่อม DB จริง เช่น Vercel Postgres / Supabase)
# DATABASE_URL=

# Admin (ถ้าต้องการเปลี่ยนจากค่าในโค้ด - ใช้เมื่อมี API จัดการ user)
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=
